extends layout

block content
    div.category
        div.topBar
            div.title 
                div.icon
                    span.mdi--pets
                h2= title

        ul.list
            each category, index in categories
                li 
                    a(href=category.url) #{index + 1}. #{category.name}

