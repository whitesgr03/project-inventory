extends layout

block content
    div.category
        div.topBar
            div.title 
                span.icon.mdi--pets
                h2= title
            div
                
                a(href='/category/create') Create Category
        

        ul.list
            each category, index in categories
                li 
                    a(href=category.url) #{index + 1}. #{category.name}

